# ===========================================================================
# AI Document Intelligence Platform — Python dependencies
# Pinned to compatible ranges. Install with:  pip install -r requirements.txt
# ===========================================================================

# --- Web framework / API ---------------------------------------------------
fastapi>=0.110,<0.116
uvicorn[standard]>=0.29
python-multipart>=0.0.9          # file uploads in FastAPI
pydantic>=2.6
pydantic-settings>=2.2           # env-driven settings

# --- Frontend --------------------------------------------------------------
streamlit>=1.32
requests>=2.31                   # frontend -> backend HTTP calls

# --- Database / ORM --------------------------------------------------------
SQLAlchemy>=2.0
# psycopg2-binary>=2.9           # uncomment when migrating to PostgreSQL

# --- Classical ML ----------------------------------------------------------
scikit-learn>=1.4
xgboost>=2.0
pandas>=2.2
numpy>=1.26
joblib>=1.3                      # model persistence

# --- NLP -------------------------------------------------------------------
nltk>=3.8
spacy>=3.7
# After install run: python -m spacy download en_core_web_sm

# --- Deep Learning embeddings / GenAI / RAG --------------------------------
sentence-transformers>=2.6
faiss-cpu>=1.8
langchain>=0.1.16
langchain-community>=0.0.34
google-generativeai>=0.5         # Gemini API

# --- Document parsing ------------------------------------------------------
pypdf>=4.0

# --- Utilities -------------------------------------------------------------
python-dotenv>=1.0
tenacity>=8.2                    # retry/backoff for external API calls

# --- Testing ---------------------------------------------------------------
pytest>=8.0
httpx>=0.27                      # FastAPI TestClient transport
